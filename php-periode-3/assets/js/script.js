function verwijder(product) {
    prompt('weet je zeker dat je het wilt verwijderen, confirm [y/n]');

    if (prompt = 'y'){
        // var $sql = 
        console.log(product);
    } else if(prompt = 'Y') {
        console.log(product);
        console.log('Y');
    } else {
        console.log('n');
    }
    // $sql = dpd("DELETE * FROM `product` where name = '". $product . "';");
} 